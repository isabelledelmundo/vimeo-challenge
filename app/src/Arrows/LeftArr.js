import React from 'react';

const LeftArr = (props) => {
    return (
        <div className='prevArrow' onClick={props.prevSlide}>
            clickLeft
        </div>
    );
}

export default LeftArr;